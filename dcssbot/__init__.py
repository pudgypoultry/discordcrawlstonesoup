"""A Discord bot that relays chat commands to a Dungeon Crawl Stone Soup game.

The bot speaks the WebTiles JSON protocol directly over a websocket — the same
protocol crawl's browser client uses — so there is no browser to automate and
no screen to scrape.
"""

__version__ = "0.1.0"
